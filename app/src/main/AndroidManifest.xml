<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gauthama.library">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".loginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainPage" />
        <activity android:name=".SignUp" />
        <activity android:name=".LookForABook" />
        <activity android:name=".Feedback" />
        <activity android:name=".LendedBooks" />
        <activity android:name=".ShareABook" />
        <activity android:name=".Sell" />
        <activity android:name=".Buy" />
        <activity android:name=".Share" />
        <activity android:name=".Display"></activity>
    </application>

</manifest>